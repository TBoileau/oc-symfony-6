<{{ tag }} class="dropdown {{ class }}">
    <{{ toggle }} class="{{ toggleClass }} dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        {% block toggle %}{% endblock %}
    </{{ toggle }}>
    <ul class="dropdown-menu dropdown-menu-{{ direction }}">
        {% block items %}{% endblock %}
    </ul>
</{{ tag }}>